天统AI京东多店经营工作台 R291 完整整合版 V5

本压缩包已经合并此前全部修复，不需要再运行任何 Fix、backup 或补丁文件夹。
V5 已完整开放京东精确白名单域名内的只读数据、资源和跨域预检请求；POST 仍按动作识别，订单、商品、库存、价格和售后等业务写入继续拦截。

第一次使用：
1. 完全关闭旧工作台；如果右下角托盘仍有工作台图标，也一并退出。
2. 把整个压缩包解压到一个全新的空文件夹，不要覆盖旧目录。
3. 双击“天统AI京东多店经营工作台.exe”。
4. 在左侧店铺后点击“打开”。
5. 如出现京东官方登录页，仅在京东页面内由员工手工完成账号、密码、短信、二维码或验证码。

注意：
- 只打开“天统AI京东多店经营工作台.exe”，不要再打开旧的修复包。
- 原来的云端设备配对和本机店铺会话会继续保留；只有客户端明确提示未配对时，才重新输入配对码。
- 当前仍是只读浏览模式，订单、商品等业务写入和自动同步保持禁用。
- 不要把京东密码、短信验证码或 Cookie 发给任何人。
